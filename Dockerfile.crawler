FROM ubuntu:24.04
RUN apt-get update && apt-get install -y --no-install-recommends \
    ca-certificates curl && rm -rf /var/lib/apt/lists/*
WORKDIR /app
COPY resources /app/resources
CMD ["/bin/bash", "-lc", "echo crawler runtime image placeholder"]
