-- Running vcpkg install
Detecting compiler hash for triplet x64-windows...
-- Automatically setting %HTTP(S)_PROXY% environment variables to "127.0.0.1:7897".
Compiler found: C:/Program Files/Microsoft Visual Studio/2022/Community/VC/Tools/MSVC/14.44.35207/bin/Hostx64/x64/cl.exe
The following packages are already installed:
  * brotli:x64-windows@1.2.0
    cli11:x64-windows@2.6.1
    cpp-httplib[brotli,core]:x64-windows@0.33.1
    cpr[core,ssl]:x64-windows@1.14.2
  * curl[core,non-http,ssl,sspi]:x64-windows@8.18.0#1
    fmt:x64-windows@12.1.0
    ftxui:x64-windows@6.1.9
    nlohmann-json:x64-windows@3.12.0#2
    replxx:x64-windows@0.0.4#1
  * sqlite3[core,json1]:x64-windows@3.51.2
    sqlitecpp[core,sqlite]:x64-windows@3.3.3
  * vcpkg-cmake:x64-windows@2024-04-23
  * vcpkg-cmake-config:x64-windows@2024-05-23
  * zlib:x64-windows@1.3.1
cli11 provides CMake targets:

  # this is heuristically generated, and may not be correct
  find_package(CLI11 CONFIG REQUIRED)
  target_link_libraries(main PRIVATE CLI11::CLI11)

cli11 provides pkg-config modules:

  # C++ command line parser
  CLI11

cpp-httplib provides CMake targets:

    find_package(httplib CONFIG REQUIRED)
    target_link_libraries(main PRIVATE httplib::httplib)

cpr provides CMake targets:

  # this is heuristically generated, and may not be correct
  find_package(cpr CONFIG REQUIRED)
  target_link_libraries(main PRIVATE cpr::cpr)

The package fmt provides CMake targets:

    find_package(fmt CONFIG REQUIRED)
    target_link_libraries(main PRIVATE fmt::fmt)

    # Or use the header-only version
    find_package(fmt CONFIG REQUIRED)
    target_link_libraries(main PRIVATE fmt::fmt-header-only)

ftxui provides CMake targets:

  # this is heuristically generated, and may not be correct
  find_package(ftxui CONFIG REQUIRED)
  target_link_libraries(main PRIVATE ftxui::dom ftxui::screen ftxui::component)

ftxui provides pkg-config modules:

  # C++ Functional Terminal User Interface.
  ftxui

The package nlohmann-json provides CMake targets:

    find_package(nlohmann_json CONFIG REQUIRED)
    target_link_libraries(main PRIVATE nlohmann_json::nlohmann_json)

The package nlohmann-json can be configured to not provide implicit conversions via a custom triplet file:

    set(nlohmann-json_IMPLICIT_CONVERSIONS OFF)

For more information, see the docs here:
    
    https://json.nlohmann.me/api/macros/json_use_implicit_conversions/

replxx provides CMake targets:

  # this is heuristically generated, and may not be correct
  find_package(replxx CONFIG REQUIRED)
  target_link_libraries(main PRIVATE replxx::replxx)

sqlitecpp provides CMake targets:

  # this is heuristically generated, and may not be correct
  find_package(SQLiteCpp CONFIG REQUIRED)
  target_link_libraries(main PRIVATE SQLiteCpp)

All requested installations completed successfully in: 620 us
-- Running vcpkg install - done
-- Selecting Windows SDK version 10.0.26100.0 to target Windows 10.0.26200.
-- Configuring done (2.8s)
-- Generating done (0.0s)
-- Build files have been written to: G:/project/shuati-Cli/build
